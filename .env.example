# Instanter environment template. Copy to .env and fill in. Never commit .env.
# Every variable listed here is read by the shipped code; the claim-drift gate
# fails CI if this file names a variable nothing reads, or omits one the code needs.

AWS_REGION=us-east-1
AWS_PROFILE=your-aws-profile
