# Private research material never reaches GitHub (standing ~/dev convention).
# The repo tracks code only. Stage named paths, never git add -A.
*.pdf
/research/

# Env + secrets
.env
.env.*
!.env.example
*.pem
*.key

# Deps + build
node_modules/
.next/
dist/
build/
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS/IDE
.DS_Store
.vscode/
.idea/
*.log

# Private working files (strategy + agent instructions stay local; never published)
/CLAUDE.md
/AGENTS.md
/.claude/
# ...and any nested one. The anchored pattern above misses mobile/.claude/,
# which a tool created there; personal assistant config is never committed.
.claude/
/docs/private/
/frontend doc
/image.png
/image-1.png
/.playwright-mcp/

# Apple push credentials (Live Activity / APNs signing keys)
*.p8
*.mobileprovision
*.cer
*.p12

# Build systems added for this project
cdk.out/
.expo/
.eas/
coverage/
htmlcov/
playwright-report/
test-results/
.coverage
.uv-cache/

# evals transient per-case run outputs (latest.json IS tracked)
evals/results/runs/

# web build artifacts
web/node_modules/
web/dist/
web/.playwright-mcp/

# local run output and screenshots
out/

# CDK / judge door build artifacts and the CloudFront origin secret
infra/.venv-cdk/
infra/cdk.out/
infra/build/
mobile/.easrc
mobile/credentials.json
infra/.door-secret
# deploy outputs: the CloudFront URL is public, the Function URL need not be
infra/cdk-outputs.json
infra/*.log

# Research + transcript working material (never published)
/DEMO VIDEO
